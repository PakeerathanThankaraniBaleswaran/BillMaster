{
  "name": "billmaster",
  "private": true,
  "version": "1.0.0",
  "description": "BillMaster monorepo runner (backend + frontend)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n backend,frontend -c yellow,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -k -n backend,frontend -c yellow,cyan \"npm --prefix backend start\" \"npm --prefix frontend run preview\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
